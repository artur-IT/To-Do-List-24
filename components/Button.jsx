export const Button = ({ add, del, done }) => {
  const buttonAdd = <button className="btn-add">Click Me</button>;
  const buttonDel = <button className="btn-del">Click Me</button>;
  const buttonDone = <button className="btn-done">Click Me</button>;
  return <>{}</>;
};
